{
  "$schema": "https://schema.zeabur.app/template.json",
  "type": "nodejs",
  "buildCommand": "pnpm install && pnpm rebuild better-sqlite3 && pnpm build",
  "startCommand": "mkdir -p /var/data && node packages/server/dist/index.js",
  "environment": {
    "NODE_ENV": "production",
    "PORT": "3000",
    "DATABASE_PATH": "/var/data/forum_tickets.db"
  },
  "nodeVersion": "20"
}
