{
  "$schema": "https://schema.zeabur.app/template.json",
  "build": {
    "command": "pnpm install && pnpm build"
  },
  "start": {
    "command": "pnpm start"
  },
  "serverless": false,
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
